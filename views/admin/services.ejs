<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Manage Services</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .container {
      max-width: 1100px;
      margin: 50px auto;
      padding: 30px;
      border-radius: 20px;
      border:1px solid  #FF6F00;
      box-shadow: 0 15px 35px rgba(250, 152, 4, 0.815);
      backdrop-filter: blur(10px);
      background: #1A1F71;
      color: #fff;
    }
    h2 { text-align: center; color: #FF6F00; margin-bottom: 25px; }
    .table { width: 100%; border-collapse: collapse; }
    .table thead { background: linear-gradient(135deg, #FF6F00, #1A1F71); }
    .table thead th { padding: 15px; text-align: center; font-weight: 600; color: #fff; }
    .table tbody td { padding: 15px; text-align: center; background: rgba(255,255,255,0.05); }
    .table tbody tr:nth-child(even) td { background: rgba(255,255,255,0.02); }
    .table tbody tr:hover td { background: rgba(255,111,0,0.2); transform: scale(1.02); cursor: pointer; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Services</h2>
    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Fee</th>
          <th>Department</th>
        </tr>
      </thead>
      <tbody>
        <% if (!services || services.length === 0) { %>
          <tr><td colspan="4">No services found.</td></tr>
        <% } else { %>
          <% services.forEach(s => { %>
            <tr>
              <td><%= s.id %></td>
              <td><%= s.name %></td>
              <td>$<%= s.fee %></td>
              <td><%= s.department_name %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
